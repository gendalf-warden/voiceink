# VoiceInk — Регрессионные тесты

Чеклист для ручного тестирования после любых изменений в коде.
Проходить **сверху вниз**, отмечая результат: OK / FAIL / SKIP.

---

## 1. Запуск и инициализация

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 1.1 | Первый запуск (нет config.json) | Конфиг создаётся в `~/.config/voiceink/config.json`, пути к whisper и модели определяются автоматически | Удалить config.json, запустить. Проверить содержимое файла |
| 1.2 | Запуск с существующим конфигом | Настройки загружаются из файла | В логе видно `Config loaded`, пути совпадают с файлом |
| 1.3 | Запуск с битым конфигом | Используются defaults, приложение не падает | Записать мусор в config.json, запустить |
| 1.4 | whisper-server стартует | Сервер запускается, в логе `Server ready` | Проверить лог |
| 1.5 | whisper-server не найден | Ошибка в логе, приложение работает (без транскрипции) | Указать неверный путь в конфиге |
| 1.6 | Ollama warmup | Модель загружается в VRAM при старте | В логе `Model qwen2.5:3b warmed up` |
| 1.7 | Ollama недоступна | Приложение работает без LLM | Остановить Ollama, запустить приложение. Лог: `Ollama available: false` |
| 1.8 | Лог-файл создаётся | `~/.config/voiceink/voiceink.log` существует | Проверить файл |
| 1.9 | Ротация лога | При >1MB старый переименовывается в `.old` | Довести лог до >1MB, перезапустить |

---

## 2. Menu bar (статус-бар)

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 2.1 | Иконка появляется | Серая иконка микрофона в menu bar | Визуально |
| 2.2 | Клик по иконке — меню | Показывается меню с заголовком, статусом, инфо, настройками | Кликнуть |
| 2.3 | Инфо в меню: горячая клавиша | Отображается текущая горячая клавиша (напр. `⌃1` или `Fn`) | Проверить текст |
| 2.4 | Инфо в меню: Whisper модель | Отображается имя модели (без `ggml-`, без `.bin`) | Проверить текст |
| 2.5 | Инфо в меню: LLM модель | При включённом Ollama — имя модели. При выключенном — `disabled` | Проверить оба варианта |
| 2.6 | Инфо в меню: язык | Отображается текущий язык (`auto`, `ru`, `en`...) | Проверить текст |
| 2.7 | Пункт Settings… (⌘,) | Открывает окно настроек | Кликнуть |
| 2.8 | Пункт Open Log… | Открывает лог-файл в системном редакторе | Кликнуть |
| 2.9 | Пункт Quit VoiceInk (⌘Q) | Приложение завершается | Кликнуть |

---

## 3. Горячая клавиша — modifier+key (любая комбинация модификатор+клавиша)

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 3.1 | Нажать и удержать горячую комбинацию | Иконка → красная анимация, лог `Recording to:` | Визуально + лог |
| 3.2 | Отпустить все клавиши одновременно | Запись останавливается → транскрипция → вставка текста | Сделать в текстовом поле |
| 3.3 | Отпустить модификатор раньше клавиши | Запись останавливается, клавиша НЕ вводится как символ | Сделать в текстовом поле — лишнего символа быть не должно |
| 3.4 | Отпустить клавишу раньше модификатора | Запись останавливается нормально | Проверить |
| 3.5 | Двойное нажатие без паузы | Первое нажатие обрабатывается, второе корректно начинает новую запись после завершения первой | Не должно быть зависания |
| 3.6 | Горячая клавиша не печатает символ | Символ клавиши не появляется в поле ввода | Фокус в текстовом поле, нажать комбинацию |
| 3.7 | Другие клавиши работают | Обычный ввод не перехватывается | Печатать текст с клавиатуры |

---

## 4. Горячая клавиша — Fn (Globe)

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 4.1 | Короткое нажатие Fn (<300ms) | Системная функция (переключение клавиатуры), запись НЕ начинается | Быстро тапнуть Fn |
| 4.2 | Долгое удержание Fn (>300ms) | Запись начинается через ~300ms, иконка → анимация | Удержать Fn |
| 4.3 | Отпустить Fn после долгого удержания | Запись останавливается → транскрипция → вставка | Проверить в текстовом поле |
| 4.4 | Fn не блокирует Emoji picker | Системная функция Fn (Globe) при коротком нажатии работает | Быстро тапнуть |

---

## 5. Запись и транскрипция

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 5.1 | Запись создаёт WAV файл | Временный файл `voiceink-*.wav` появляется в /tmp | Проверить лог `Recording to:` |
| 5.2 | Формат аудио: 16kHz mono 16bit PCM | whisper-server принимает файл без ошибок | Транскрипция проходит |
| 5.3 | Русская речь → русский текст | Транскрибируется на русском, НЕ переводится на английский | Надиктовать по-русски |
| 5.4 | Английская речь → английский текст | Транскрибируется на английском | Надиктовать по-английски |
| 5.5 | Смешанная речь (рус + англ) | Оба языка транскрибируются корректно | Надиктовать фразу с переключением |
| 5.6 | Пустая запись (тишина) | Состояние возвращается в idle, ничего не вставляется | Нажать и сразу отпустить |
| 5.7 | Длинная запись (30+ сек) | Транскрибируется полностью | Надиктовать длинный текст |
| 5.8 | Временный файл удаляется после транскрипции | Файл в /tmp удалён | Проверить filesystem |
| 5.9 | whisper-server не запущен | Ошибка `Whisper server not running`, возврат в idle через 3 сек | Убить процесс whisper-server, попробовать записать |

---

## 6. LLM-постпроцессинг

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 6.1 | Текст проходит через LLM | Пунктуация и заглавные буквы добавлены | Надиктовать без пауз, проверить результат |
| 6.2 | LLM не меняет слова | Слова остаются те же, только пунктуация | Сравнить raw и processed в логе |
| 6.3 | LLM не переводит текст | Русский остаётся русским, английский — английским | Проверить в логе |
| 6.4 | LLM упал во время запроса | Используется raw текст, лог `LLM failed:` | Прибить процесс во время транскрипции |
| 6.5 | Постпроцессинг отключён | Вставляется raw текст без задержки на LLM | `dictationMode: "off"`, проверить скорость |
| 6.6 | Состояние UI при постпроцессинге | Иконка меняется на фиолетовую (brain) во время LLM | Визуально |

### 6.7. Режимы пост-обработки (mode regression)

Каждый режим протестировать на эталонной фразе. Эталоны должны содержать:
- цифры («у меня 5 котов и 3 собаки»),
- англицизмы внутри русского («открой API в Chrome»),
- спецсимволы / пунктуацию («запятые точки и тире»).

| # | Режим | Эталон (надиктовать) | Ожидание |
|---|-------|----------------------|----------|
| 6.7.1 | Off | «у меня 5 котов и 3 собаки» | Raw whisper-вывод без LLM, цифры цифрами |
| 6.7.2 | Punctuation (RU) | «у меня 5 котов и 3 собаки также есть попугай» | «У меня 5 котов и 3 собаки. Также есть попугай.» — слова не меняются, цифры цифрами |
| 6.7.3 | Punctuation (EN) | «i have 5 cats and 3 dogs» | «I have 5 cats and 3 dogs.» — слова не меняются |
| 6.7.4 | Punctuation (mixed) | «открой API в Chrome и проверь endpoint» | API/Chrome/endpoint сохранены, добавлена пунктуация |
| 6.7.5 | Grammar (RU) | «мой книги новые и интересный» | «Мои книги новые и интересные.» — падежи/согласования исправлены, без перевода |
| 6.7.6 | List (RU) | «купить хлеб молоко и яйца также не забыть про сыр» | Маркированный список «- хлеб, - молоко, - яйца, - сыр» |
| 6.7.7 | List (EN) | «first buy bread second milk and also eggs» | «- bread, - milk, - eggs» |
| 6.7.8 | Translate RU→EN | «привет мир как дела» | «Hello world, how are you?» (или близкий вариант), цифры сохранены |
| 6.7.9 | Translate EN→RU | «hello world how are you» | «Привет, мир, как ты?» (или близкий вариант) |
| 6.7.10 | Translate уже на target | надиктовать на английском с режимом translate→en | Текст не сломан, может быть лишь подправлена пунктуация |
| 6.7.11 | Hallucination guard (length) | надиктовать «привет» в режиме grammar; если LLM выдаст пояснение — должен сработать 3× guard | В логе `output too long`, вставляется raw |
| 6.7.12 | Script guard (punctuation на RU) | надиктовать русский текст; если LLM ответит на английском — должен сработать script guard | В логе `LLM changed script — using raw`, вставляется русский raw |
| 6.7.13 | Translate не триггерит script guard | translate RU→EN на русской фразе | Английский перевод вставляется, в логе **нет** `LLM changed script` |
| 6.7.14 | Меню статусбара показывает текущий режим | Под Whisper-моделью два пункта «Dictation: X» и «File: Y» с подменю | Кликнуть в menu bar |
| 6.7.15 | Смена режима из меню | Подменю показывает чекмарк на текущем, выбор сохраняется в config.json | Изменить, перезапустить — должно остаться |
| 6.7.16 | Файл-транскрипция в режиме translate | Файл с русской речью + fileMode=translate, target=en → SRT/TXT на английском | Прогнать файл в `Transcribe File…` |
| 6.7.17 | Backward compat config | Старый config 0.3b с `"punctuationEnabled": true` → `dictationMode=punctuation` после загрузки | Подложить старый JSON, проверить лог `dictation: punctuation` |

---

## 7. Вставка текста

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 7.1 | Текст вставляется в активное поле | Результат появляется в текстовом поле | TextEdit, Telegram, VS Code, браузер |
| 7.2 | Буфер обмена восстанавливается | Через 150ms clipboard содержит то, что было ДО диктовки | Скопировать что-то, надиктовать, подождать, Cmd+V |
| 7.3 | Работает с разными раскладками | Cmd+V работает на QWERTY, Colemak, русской раскладке | Переключить раскладку, надиктовать |
| 7.4 | Работает в разных приложениях | Текст вставляется в: TextEdit, Chrome, Telegram, Terminal, VS Code | Проверить каждое |

---

## 8. Окно настроек

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 8.1 | Окно открывается из меню | Плавающее окно 380×230, поверх других окон | Кликнуть Settings… |
| 8.2 | Повторное открытие — то же окно | Не создаётся новое, фокус на существующее | Открыть, закрыть, открыть |
| 8.3 | Отображается текущая горячая клавиша | Поле показывает `⌃1` или `Fn` — то, что в конфиге | Визуально |
| 8.4 | Клик по полю горячей клавиши | Поле синеет, текст `Press a key combo or Fn…` | Кликнуть |
| 8.5 | Ввод новой комбинации (напр. ⌘⇧A) | Комбинация сохраняется, поле показывает новую, лог `Hotkey changed` | Нажать новую комбинацию |
| 8.6 | Ввод Fn как горячей клавиши | Поле показывает `Fn`, конфиг сохранён | Тапнуть Fn в режиме записи |
| 8.7 | Escape отменяет запись | Выход из режима записи, старое значение сохраняется | Кликнуть по полю, нажать Esc |
| 8.8 | Голая клавиша без модификатора отклоняется | Нажатие `A` без модификаторов не принимается | В режиме записи нажать просто `A` |
| 8.9 | Горячая клавиша перезагружается без рестарта | Новая комбинация работает сразу | Сменить клавишу, проверить push-to-talk |
| 8.10 | Чекбокс Launch at Login | Переключает, сохраняет в конфиг, создаёт/удаляет LaunchAgent | Переключить, проверить `~/Library/LaunchAgents/com.voiceink.app.plist` |
| 8.11 | Чекбокс Log transcription text | Переключает, сохраняет в конфиг | Переключить, проверить лог — текст маскирован `[N chars]` |

---

## 9. Логирование

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 9.1 | Логи пишутся в файл | Записи появляются в `~/.config/voiceink/voiceink.log` | Открыть файл |
| 9.2 | Формат: `timestamp [tag] message` | Пример: `2026-04-05 12:34:56.789 [VoiceInk] State: Ready` | Открыть файл |
| 9.3 | Логи дублируются в stdout | При запуске из терминала логи видны | Запустить из терминала |
| 9.4 | logTranscriptions=true | В логе виден полный текст транскрипции | Проверить лог |
| 9.5 | logTranscriptions=false | В логе `[N chars]` вместо текста | Отключить, надиктовать, проверить лог |
| 9.6 | Тайминги в логе | Время ASR и LLM отображается: `Raw (1.2s):`, `Processed (0.8s):` | Проверить лог |

---

## 10. Жизненный цикл и стабильность

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 10.1 | Quit из меню | whisper-server останавливается, Ollama модель выгружается | Quit, проверить `ps aux | grep whisper`, `ollama ps` |
| 10.2 | SIGINT/SIGTERM | Корректное завершение | `kill <pid>` |
| 10.3 | Ошибка → возврат в idle | После ошибки через 3 сек состояние = idle | Вызвать ошибку, подождать |
| 10.4 | Запись во время транскрипции | Игнорируется (guard state == .idle) | Нажать горячую клавишу пока идёт обработка |
| 10.5 | Многократная диктовка подряд | 10+ диктовок без утечек и зависаний | Повторить 10 раз |
| 10.6 | Event tap восстанавливается | После `tapDisabledByTimeout` tap включается обратно | Длительная работа под нагрузкой |
| 10.7 | Нет зомби-процессов whisper-server | После Quit процесс whisper-server не остаётся | `ps aux | grep whisper-server` |

---

## 11. Состояния UI (переходы)

Все переходы между состояниями:

```
idle → recording → transcribing → postProcessing → idle
idle → recording → transcribing → idle  (без LLM)
idle → recording → transcribing → error → idle  (ошибка ASR)
idle → recording → transcribing → postProcessing → error → idle  (ошибка LLM → raw text)
idle → error → idle  (server not running)
```

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 11.1 | idle | Серая иконка, tooltip `Ready` | Визуально |
| 11.2 | recording | Красная анимация (бегущие полоски), tooltip `Recording...` | Удержать горячую клавишу |
| 11.3 | transcribing | Синяя иконка (waveform), tooltip `Transcribing...` | Отпустить горячую клавишу |
| 11.4 | postProcessing | Фиолетовая иконка (brain), tooltip `Post-processing...` | Отпустить горячую клавишу (с включённым Ollama) |
| 11.5 | error | Красный треугольник, tooltip `Error` | Вызвать ошибку |
| 11.6 | error → idle через 3 сек | Автоматический возврат | Подождать |

---

## 12. Конфигурация

| # | Тест | Ожидание | Как проверить |
|---|------|----------|---------------|
| 12.1 | config.json — pretty-printed JSON | Файл читаем, ключи отсортированы | Открыть файл |
| 12.2 | Новые поля с defaults | Старый конфиг без `launchAtLogin`/`logTranscriptions` загружается с defaults (false/true) | Удалить эти поля из JSON, запустить |
| 12.3 | Сохранение из Settings | Изменения записываются в JSON мгновенно | Изменить настройку, проверить файл |
| 12.4 | Auto-detect whisper paths | При отсутствии путей — поиск в стандартных местах | Удалить конфиг, запустить |

---

## Как использовать

### При каждом изменении кода
1. Пересобрать: `swift build`
2. Пройти тесты **затронутого раздела** + раздел 10 (стабильность)

### При рефакторинге / крупных изменениях
1. Пересобрать: `swift build`
2. Пройти **все разделы** 1–12

### Приоритетный быстрый прогон (smoke test)
Минимальный набор для проверки базовой работоспособности:

1. **1.2** — запуск с конфигом
2. **2.2** — меню открывается
3. **3.1 + 3.2** — push-to-talk работает
4. **5.3** — русская речь транскрибируется по-русски
5. **6.1** — LLM добавляет пунктуацию
6. **7.1** — текст вставляется
7. **10.1** — Quit корректно завершает всё
