<?xml version="1.0" standalone="yes"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
    <channel>
        <title>VoiceInk</title>
        <link>https://gendalf-warden.github.io/voiceink/</link>
        <description>VoiceInk macOS dictation app — release feed</description>
        <language>en</language>
        <item>
            <title>Version 0.5.013</title>
            <pubDate>Fri, 05 Jun 2026 22:38:51 +0000</pubDate>
            <sparkle:version>0.5.013</sparkle:version>
            <sparkle:shortVersionString>0.5.013</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Added — field diagnostics</h3>
<p>Triaging Anna&#x27;s report meant *inferring* almost everything (version from bundle</p>
<p>paths, &quot;Thank you.&quot; from a <code>[10 chars]</code> count, silence from a duration/length</p>
<p>ratio). These logs make the next report readable instead of guessable. Privacy</p>
<p>principle: dropped/filtered text is rejected junk (never inserted), so it&#x27;s</p>
<p>logged verbatim; real dictated text stays behind the <code>logTranscriptions</code> opt-in.</p>
<ul>
  <li><strong>Startup diagnostic banner</strong>: one block per launch with app version+build,</li>
</ul>
<p>  chip model (→ which ggml CPU backend applies), macOS version, RAM, microphone</p>
<p>  &amp; accessibility permission states, hotkey, modes, and — crucially for the</p>
<p>  &quot;no backends&quot; class — the list of ggml backend <code>.so</code> bundled next to</p>
<p>  llama-server (<code>NONE ⚠️</code> if missing).</p>
<ul>
  <li><strong>Recording stats</strong>: every stop logs duration + peak amplitude, so a</li>
</ul>
<p>  silence-driven hallucination is obvious (<code>4.8s, peak 0.004</code>). Skip reasons</p>
<p>  (too-short / silent) now include the numbers.</p>
<ul>
  <li><strong>Filter/drop visibility</strong>: <code>Transcriber</code> logs what the hallucination filter</li>
</ul>
<p>  removed (<code>dropped entire output: &quot;Thank you.&quot;</code>); the pipeline logs when a</p>
<p>  transcription is empty after filtering (the phantom-text path) — confirms the</p>
<p>  0.5.011 fixes are firing in the field.</p>
<ul>
  <li><strong>Hotkey trigger classification</strong>: logs <code>Fn held ≥300ms → push-to-talk</code> vs</li>
</ul>
<p>  <code>Fn+key combo — push-to-talk cancelled</code>, so accidental triggers are visible.</p>
<ul>
  <li><strong>Redacted transcription signature</strong>: <code>[N chars, M words]</code> instead of bare</li>
</ul>
<p>  <code>[N chars]</code> when full-text logging is off.</p>

<h3>Fixed — build warnings (0-warnings gate)</h3>
<ul>
  <li>Cleared all 6 pre-existing <code>#SendableClosureCaptures</code> warnings so <code>develop</code></li>
</ul>
<p>  passes CLAUDE.md&#x27;s &quot;swift build — 0 warnings&quot; gate on a *clean* build</p>
<p>  (<code>rm -rf .build &amp;&amp; swift build</code>). <code>AppDelegate</code> error-tail now uses</p>
<p>  <code>Task.sleep</code> + <code>MainActor.run</code> instead of a <code>@Sendable</code></p>
<p>  <code>DispatchQueue.asyncAfter</code> closure; <code>AudioConverter</code>&#x27;s <code>requestMediaDataWhenReady</code></p>
<p>  callback (which runs serially on a dedicated queue) rebinds its AVFoundation</p>
<p>  objects <code>nonisolated(unsafe)</code>. No behavior change. (Note: only 2 showed in</p>
<p>  incremental builds — the other 4 in <code>AudioConverter</code> only surface on a clean</p>
<p>  build because that file isn&#x27;t otherwise recompiled.)</p>]]></description>
            <enclosure
                url="https://gendalf-warden.github.io/voiceink/VoiceInk-0.5.013.dmg"
                length="12989278"
                type="application/octet-stream"
                sparkle:edSignature="rqCsRiND8gvNfPNLLmjAgEK7yEzQ2x/4UO80Z8X6bjikBLyzPbkHskYmjX2yMbmqdhiWFCouL2LCT9PJ1UmOBg==" />
        </item>
    </channel>
</rss>
